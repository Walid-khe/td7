phpunit:
  name: Tests PHPUnit
  runs-on: ubuntu-latest

  steps:
    - name: Récupérer le code
      uses: actions/checkout@v2

    - name: Configuration de PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: "8.2"

    - name: Exécuter les tests PHPUnit
      run: php vendor/bin/phpunit --testsuite=tests
